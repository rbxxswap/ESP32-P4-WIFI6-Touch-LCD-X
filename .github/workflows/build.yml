name: ESP-IDF Build

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build esp_brookesia_phone (Waveshare LCD-X) for ESP32-P4
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Cache ccache + managed_components
        uses: actions/cache@v4
        with:
          path: |
            ~/.ccache
            ~/.cache/ccache
            examples/esp-idf/11_esp_brookesia_phone/managed_components
            examples/esp-idf/11_esp_brookesia_phone/dependencies.lock
          key: esp32p4-${{ runner.os }}-${{ hashFiles('examples/esp-idf/11_esp_brookesia_phone/main/idf_component.yml', 'examples/esp-idf/11_esp_brookesia_phone/CMakeLists.txt', 'examples/esp-idf/11_esp_brookesia_phone/sdkconfig.defaults') }}
          restore-keys: |
            esp32p4-${{ runner.os }}-

      - name: ESP-IDF build + merge (v5.5.3, esp32p4)
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5.3
          target: esp32p4
          path: examples/esp-idf/11_esp_brookesia_phone
          command: "idf.py build && idf.py merge-bin -o firmware-merged-esp32p4.bin"

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-esp32p4
          path: |
            examples/esp-idf/11_esp_brookesia_phone/build/firmware-merged-esp32p4.bin
            examples/esp-idf/11_esp_brookesia_phone/build/*.bin
            examples/esp-idf/11_esp_brookesia_phone/build/*.elf
            examples/esp-idf/11_esp_brookesia_phone/build/bootloader/bootloader.bin
            examples/esp-idf/11_esp_brookesia_phone/build/partition_table/partition-table.bin
            examples/esp-idf/11_esp_brookesia_phone/build/flasher_args.json
          if-no-files-found: warn

      - name: Attach binaries to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            examples/esp-idf/11_esp_brookesia_phone/build/firmware-merged-esp32p4.bin
            examples/esp-idf/11_esp_brookesia_phone/build/11_esp_brookesia_phone.bin
            examples/esp-idf/11_esp_brookesia_phone/build/bootloader/bootloader.bin
            examples/esp-idf/11_esp_brookesia_phone/build/partition_table/partition-table.bin
            examples/esp-idf/11_esp_brookesia_phone/build/flasher_args.json
